{
    "name": "standardbot",
    "version": "1.0.0",
    "dependencies": {
        "@standard-schema/spec": "^1.0.0"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^3.0.8",
        "valibot": "^1.0.0-rc.4",
        "vitest": "^3.0.8"
    },
    "scripts": {
        "test": "vitest --coverage"
    }
}
